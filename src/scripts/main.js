jQuery( function( $ ) {
	// let imgover = require( './imgover.js' );
	let effect  = require( './effect.js' );
	let masonry = require( './masonry.min.js' );
	let config  = require( './config.js' );
} );
